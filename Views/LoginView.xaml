<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProvaMVVM.Views.LoginView"
             Title="LoginView">

    <StackLayout Padding="30" Spacing="20" VerticalOptions="Center">

        <Label Text="Login" FontSize="24" FontAttributes="Bold" HorizontalOptions="Center"/>

        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
        <Entry Placeholder="Senha" Text="{Binding Senha}" IsPassword="True" />

        <StackLayout Spacing="15" Margin="0,10,0,0">
            <Button Text="Entrar" Command="{Binding LoginCommand}" HeightRequest="50" CornerRadius="10"/>
            <Button Text="Registre-se" Command="{Binding IrParaCadastroCommand}" HeightRequest="50" CornerRadius="10"/>
        </StackLayout>

    </StackLayout>
</ContentPage>
